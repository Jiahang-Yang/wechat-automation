# 公众号工作流 · 使用者指南

这份文件夹是帮您**写公众号文章、自动排版、一键推进草稿箱**的工具。您不需要懂里面的技术，只要知道「平时动哪些、哪些别碰」就行。

---

## 一、您平时只用这两个文件夹

| 文件夹 | 您做什么 | 说明 |
|--------|----------|------|
| **文章草稿/** | 看稿、改稿、放新稿 | 每篇文章一个 `.md` 文件（纯文本，好改）；旁边同名的 `.wechat.html` 是排好版的成品，别手改 |
| **素材/** | 放配图 | 文章要用的图片放这里（jpg / png / webp 都行） |

> 写稿这事通常让儿子（或小 AI 助手）帮写，写好后存进 `文章草稿/`，您再检查、定稿。

---

## 二、发一篇文章，三步

> 前提：脚本和排版已经由儿子帮您装好（第一次需要微信扫码登录一次，之后免登）。

| 步 | 谁做 | 在哪做 | 操作 |
|----|------|--------|------|
| ① 排版 | 您（或助手） | 电脑命令行 | 跑一条命令，把 `.md` 变成排好版的 `.wechat.html` |
| ② 推草稿 | 您（或助手） | 电脑命令行 | 跑一条命令，把 `.wechat.html` 推进公众号**草稿箱** |
| ③ 群发 | **您** | 微信后台 | 登录 mp.weixin.qq.com → 草稿箱 → 预览 → 群发 / 定时群发 |

**为什么最后一步必须您自己点**：个人订阅号没有自动群发的权限（微信规定的），所以「发出去」这个动作只能您本人在后台确认。前面 90% 的活儿都已经自动化了。

---

## 三、命令怎么跑（给助手 / 您照抄）

> 想看进度？直接让助手（小 AI）帮您跑，**他会在对话里一步步报告进度**（启动浏览器 → 登录检查 → 抓取列表），您看着对话就行，不用碰文件夹。

打开 **Git Bash**（装了环境就有），进入 `工具` 文件夹后，打这两行：

```bash
# 第①步：生成排版（xxx 换成文章名，不含后缀；以下两行都在 工具/ 文件夹里运行）
../venv/Scripts/python.exe md2wechat.py ../文章草稿/xxx.md

# 第②步：推进草稿箱
./browser/publish.sh xxx
```

跑完看到结尾出现 `appmsgid = 1000000xx` 就是成功了。

> 如果助手帮您操作，他直接用 `工具/browser/publish.sh xxx` 即可，不用您管细节。

---

## 四、那些「看不懂的文件夹，您别动」

| 文件夹 / 文件 | 是什么 | 您要做 |
|---------------|--------|--------|
| **工具/** | 所有脚本、登录信息、依赖（分 api/ 与 browser/ 两组，技术核心） | **别动**，交给助手维护 |
| **venv/** | Python 运行环境（上百 MB） | **别动、别删**，删了发文就废 |
| **_archive/** | 以前放弃的企业号资料 | 不用管 |
| **README.md** | 给技术接手人看的说明 | 不用看 |
| **技能/** | 给 AI 助手用的技能（写作/排版/发布，多个子目录） | 不用看 |
| **.gitignore** | 版本管理配置 | 不用看 |

---

## 五、几个要点提醒

- **排版统一**：文章只走「写 `.md` → 跑脚本 → 复制 `.wechat.html` → 微信粘贴」这一条路。别把 `.md` 当纯文本直接贴进微信，会变成默认样式、和成品对不上。
- **标题不重复**：`.md` 第一行 `# 文章标题` 只是标题，不进正文；微信「标题栏」请手填这行字，否则标题会显示两遍。
- **卡死处理**：用**微信 PC 版**内嵌浏览器粘贴最稳；用 Edge 浏览器开后台可能卡死（是 Dark Reader 扩展的锅）。
- **定时群发**：后台支持最长 2 天内的定时群发，设好时间到点自动发，不用守着。
- **合规**：财税内容定位「梳理 / 分享 / 教育」，不写"包过核查""保证节税 X%"这类承诺；不针对具体股票基金给买卖建议；标注"个人观点、仅供参考"反而更显专业。

---

## 六、账号定位

- **运海老杨**（您的号）：财税健康 / 个人财税 / 从业者视角，认真做内容，关联书系《财税创造价值》。
- 内容方向与风格基线见 `README.md` 第八节；写作时遵循 `技能/写作` 的规则。
